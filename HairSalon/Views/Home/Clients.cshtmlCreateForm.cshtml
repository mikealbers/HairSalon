@{
    Layout = "_Layout";
}

<div class="jumbotron text-center">
    <h1>Clients</h1>
</div>
<div class="container text-center">
  <h1>New Client</h1>
  <form action="/clients" method="post">
    <input type="text" name="firstName"></input>
    <input type="text" name="lastName"></input>
    <select class="form-dropdown" name="stylistID" required>
      @foreach (var stylist in Model)
      {
        <option value="@stylist.GetStylistID()">@stylist.GetStylistFirstName() @stylist.GetStylistLastName()</option>
      }
    </select>

    </select>
    <button type="submit">Add a new CLIENT</button>
  </form>

  <p><a href="/">Go Back</a></p>
  <p><a href="/stylists">STYLIST List</a></p>

</div>
